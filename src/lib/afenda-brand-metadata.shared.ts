/**
 * Canonical URLs for OG / Twitter imagery and parity with favicon manifests.
 * Asset paths mirror `pnpm icons:generate` output under `public/icons/`.
 */
export const AFENDA_METADATA_IMAGE = {
  url: "/icons/afenda-icon-512-transparent.png",
  width: 512,
  height: 512,
} as const;

export const AFENDA_METADATA_IMAGE_URL = AFENDA_METADATA_IMAGE.url;
